# Template for .env. Commit this file. Copy to .env and fill in values.
# Anything prefixed EXPO_PUBLIC_ is bundled into the JS — don't put secrets there.

# Path to your Google Play service account JSON (used by `eas submit`).
GOOGLE_PLAY_SERVICE_ACCOUNT=./play-service-account.json
